# Fake TLS با پورت‌های دلخواه - راهنمای جامع

## 🎯 معرفی

حالا می‌توانید Fake TLS پروکسی‌ها را با **پورت‌های دلخواه** بسازید! این قابلیت به شما امکان می‌دهد از پورت‌های مختلف برای دور زدن فیلترها استفاده کنید.

## 🚀 پورت‌های پیشنهادی

### پورت‌های محبوب برای Fake TLS:
- **443** - استاندارد HTTPS (توصیه شده)
- **8443** - HTTPS جایگزین (بسیار محبوب)
- **2083** - cPanel SSL (معمولاً باز است)
- **2087** - WHM SSL (معمولاً باز است)
- **8880** - جایگزین HTTPS
- **2053** - پورت خاص SSL

### پورت‌های مناسب برای انواع دیگر:
- **8080** - HTTP جایگزین
- **8443** - HTTPS جایگزین
- **8888** - پورت توسعه
- **2052** - پورت خاص

## ⚙️ نحوه استفاده

### 1. از طریق پنل وب:

1. وارد پنل HoseinProxy شوید
2. روی "پروکسی جدید" کلیک کنید
3. **نوع پروتکل** را "Fake TLS (Anti-Filter)" انتخاب کنید
4. **پورت دلخواه** خود را وارد کنید
5. **دامنه TLS** را انتخاب کنید (مثلاً google.com)
6. ذخیره کنید!

### 2. از طریق manage.sh:

```bash
# اجرای اسکریپت مدیریت
./manage.sh

# انتخاب گزینه 12: Install Fake TLS Support
# یا
# انتخاب گزینه 11: Build Fake TLS Image
```

## 🔧 پیکربندی پورت در backend

### برای پروکسی‌های جدید:
```python
# در proxy.py - ایجاد پروکسی جدید
if proxy_type == "tls":
    # برای Fake TLS از پورت یکسان داخلی و خارجی استفاده کن
    ports_config = {f'{port}/tcp': port}
```

### برای ویرایش پروکسی‌ها:
```python
# در proxy.py - به‌روزرسانی پروکسی
if proxy.proxy_type == "tls":
    ports_config = {f'{proxy.port}/tcp': proxy.port}
```

## 🛡️ ویژگی‌های امنیتی

### 1. تقلید TLS کامل:
- Handshake واقعی TLS 1.2
- Certificate معتبر به نظر می‌رسد
- زمان‌بندی تصادفی برای جلوگیری از تشخیص

### 2. دامنه استتار:
- از دامنه‌های محبوب استفاده می‌کند
- ترافیک شبیه HTTPS واقعی است
- قابل تنظیم برای هر دامنه‌ای

### 3. پدینگ اتصال:
- داده‌های تصادفی اضافه می‌کند
- جلوگیری از تحلیل ترافیک
- هر اتصال منحصر به فرد است

## 📊 مزایای پورت‌های دلخواه

### ✅ دور زدن فیلترهای پیشرفته:
- **تنوع پورت**: از شناسایی الگوها جلوگیری می‌کند
- **پورت‌های خاص**: معمولاً فیلتر نمی‌شوند
- **تغییر آسان**: سریع می‌توانید پورت را عوض کنید

### ✅ سازگاری بیشتر:
- **سرویس‌های مختلف**: با تنظیمات مختلف سازگار است
- **محدودیت‌های شبکه**: دور زدن محدودیت‌های ISP
- **تنظیمات فایروال**: انعطاف‌پذیری بیشتر

## 🧪 تست و عیب‌یابی

### تست اتصال:
```bash
# تست TLS handshake
openssl s_client -connect your-server:your-port -servername google.com

# تست پروکسی
telnet your-server your-port
```

### بررسی لاگ‌ها:
```bash
# از طریق manage.sh
./manage.sh
# گزینه 10: Fake TLS Management
# گزینه 3: View Fake TLS Logs
```

### بررسی وضعیت:
```bash
# بررسی وضعیت سیستم
./manage.sh
# گزینه 13: System Status Check
```

## 🚨 نکات مهم

### 1. انتخاب پورت مناسب:
- **از پورت‌های رایج استفاده کنید**
- **پورت‌های بالای 1024 نیاز به root ندارند**
- **پورت‌های زیر 1024 نیاز به دسترسی root دارند**

### 2. امنیت:
- **از پورت‌های تصادفی اجتناب کنید**
- **پورت‌های مشکوک را استفاده نکنید**
- **همیشه تست کنید که پورت باز است**

### 3. عملکرد:
- **پورت‌های کم‌ترافیک بهتر عمل می‌کنند**
- **پورت‌های استاندارد سریع‌تر شناسایی می‌شوند**
- **تست کنید که ISP پورت را مسدود نکرده**

## 🔍 بررسی پورت‌های باز

### در سرور لینوکس:
```bash
# بررسی پورت‌های باز
netstat -tlnp | grep your-port

# بررسی فایروال
ufw status
iptables -L
```

### تست از راه دور:
```bash
# تست با nmap
nmap -p your-port your-server

# تست با telnet
telnet your-server your-port
```

## 🎉 مثال‌های کاربردی

### سناریو 1: شبکه محدود
```
نوع: Fake TLS
پورت: 2083
دامنه: cloudflare.com
کاربرد: دور زدن محدودیت‌های سازمانی
```

### سناریو 2: ISP سخت‌گیر
```
نوع: Fake TLS  
پورت: 8443
دامنه: google.com
کاربرد: دور زدن فیلترینگ ISP
```

### سناریو 3: تست و توسعه
```
نوع: Fake TLS
پورت: 8880
دامنه: microsoft.com
کاربرد: تست و توسعه
```

## 📞 پشتیبانی

اگر مشکلی دارید:

1. **لاگ‌ها را بررسی کنید**
2. **وضعیت سیستم را چک کنید**
3. **پورت را تست کنید**
4. **دامنه را عوض کنید**
5. **از پورت دیگری استفاده کنید**

---

**🎉 حالا می‌توانید Fake TLS را با هر پورتی که می‌خواهید بسازید!**